{
  "name": "mern_reddit",
  "version": "1.0.0",
  "description": "Forum creation practice",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm run server --prefix server",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "khyzad",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^4.0.1"
  }
}
